pub struct BlackboardManager extends Decorator{
    blackboard: Blackboard = null
}

fn run(blackboard: Blackboard) -> bool{
    newBlackboard = Blackboard()::new();
    newBlackboard.parent = blackboard;
    result = child.run();
    newBlackboard == null;
    return result
    
}
